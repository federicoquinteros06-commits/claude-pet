[pytest]
# El proyecto no es un paquete: los modulos viven en la raiz y los tests en
# tests/. Sin esto pytest solo mete tests/ en sys.path y `import claude_pet`
# falla.
pythonpath = .
testpaths = tests
